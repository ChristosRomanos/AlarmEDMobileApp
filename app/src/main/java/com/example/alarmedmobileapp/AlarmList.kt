package com.example.alarmedmobileapp

class AlarmList(
    val title: String, // Name of the alarm list
    var alarms: MutableList<Alarm> // List of alarms (can be replaced with a custom Alarm class if needed)
)

